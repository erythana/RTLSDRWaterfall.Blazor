// import * as waterfall from "./waterfall";

export function createWaterfall(args) {
    console.log("test");
    // return new waterfall.Waterfall(id, dataURL, annotationURL, isAnimatable, isSelectable, isZoomable);
}